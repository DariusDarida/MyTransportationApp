@page
@model MyTransportationApp.Pages.Trains.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Train</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Train.ID" />
            <div class="form-group">
                <label asp-for="Train.TrainCode" class="control-label"></label>
                <input asp-for="Train.TrainCode" class="form-control" />
                <span asp-validation-for="Train.TrainCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Train.TransporterID" class="control-label"></label>
                <select asp-for="Train.TransporterID" class="form-control" asp-items="ViewBag.TransporterID"></select>
                <span asp-validation-for="Train.TransporterID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
